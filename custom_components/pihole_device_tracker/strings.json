{
  "config": {
    "step": {
      "user": {
        "title": "Configure Pi-hole Device Tracker",
        "description": "Set up your Pi-hole v6.0+ instance for device tracking",
        "data": {
          "host": "Host",
          "password": "Web Interface Password",
          "scan_interval": "Poll Interval (seconds)",
          "away_time": "Consider Away After (seconds)"
        },
        "data_description": {
          "host": "IP address or hostname of your Pi-hole (e.g., 192.168.1.100 or pi.hole)",
          "password": "Leave empty if no password is set",
          "scan_interval": "How often to check Pi-hole for device updates (minimum 5 seconds)",
          "away_time": "Time without DNS activity before marking device as away (900 seconds = 15 minutes)"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to Pi-hole. Check the host and network connectivity.",
      "unknown": "An unexpected error occurred. Check the logs."
    },
    "abort": {
      "already_configured": "This Pi-hole instance is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Update Pi-hole Device Tracker Options",
        "description": "Configure SSH access to Pi-hole for ARP table monitoring",
        "data": {
          "ssh_host": "SSH Host (Pi-hole IP)",
          "ssh_port": "SSH Port",
          "ssh_username": "SSH Username",
          "ssh_password": "SSH Password",
          "ssh_key_path": "SSH Private Key Path",
          "scan_interval": "Poll Interval (seconds)",
          "away_time": "Consider Away After (seconds)"
        },
        "data_description": {
          "ssh_host": "IP address of your Raspberry Pi (e.g., 192.168.8.100)",
          "ssh_port": "SSH port (default 22)",
          "ssh_username": "Username for SSH connection (e.g., pi)",
          "ssh_password": "Password for SSH (leave empty if using key)",
          "ssh_key_path": "Path to private key file (leave empty if using password)",
          "scan_interval": "How often to check Pi-hole for device updates",
          "away_time": "Time without DNS activity before marking device as away"
        }
      }
    }
  }
}