# Pi-hole Device Tracker

Интеграция Home Assistant для отслеживания устройств в сети с помощью Pi-hole v6.0+.

## Возможности

- 🔍 Отслеживание устройств на основе DNS-активности в Pi-hole
- 🏠 Интеграция с платформой device tracker в Home Assistant
- ⚙️ Настраиваемые интервалы обновления (минимум 5 секунд)
- 🔐 Аутентификация по паролю для веб-интерфейса Pi-hole
- 📊 Подробная информация об устройстве (имя, IP, производитель MAC, время последнего запроса)
- 🌐 Поддержка нескольких языков (EN, RU, DE)

## Установка

### Через HACS

1. Откройте Home Assistant
2. Перейдите в HACS → Интеграции
3. Нажмите кнопку "+"
4. Найдите "Pi-hole Device Tracker"
5. Нажмите "Установить"
6. Перезапустите Home Assistant

### Ручная установка

1. Скачайте репозиторий как ZIP
2. Распакуйте в `config/custom_components/pihole_device_tracker/`
3. Перезапустите Home Assistant
4. Перейдите в Настройки → Устройства и службы
5. Нажмите "Добавить интеграцию"
6. Найдите "Pi-hole Device Tracker"

## Настройка

### Процесс настройки

1. Перейдите в Настройки → Устройства и службы
2. Нажмите "Добавить интеграцию"
3. Выберите "Pi-hole Device Tracker"
4. Введите:
   - **Хост**: IP-адрес или имя хоста Pi-hole (например, `192.168.1.100` или `pi.hole`)
   - **Пароль**: Пароль веб-интерфейса (оставьте пустым, если пароля нет)
   - **Интервал опроса**: Как часто проверять Pi-hole (по умолчанию: 30 секунд, мин.: 5)
   - **Считать ушедшим через**: Время без DNS-активности перед пометкой устройства как ушедшего (по умолчанию: 900 секунд = 15 минут)

## Использование

После настройки устройства появятся в Home Assistant как сущности device tracker.

### Идентификаторы сущностей

Идентификаторы генерируются на основе имени устройства + последние 4 символа MAC + "pihole":
- `device_tracker.iphone_a1b2_pihole`
- `device_tracker.macbook_c3d4_pihole`
- `device_tracker.192_168_1_50_pihole` (если имя недоступно)

### Атрибуты

Каждый трекер включает дополнительные атрибуты:
- `last_query`: Метка времени последнего DNS-запроса
- `last_query_seconds_ago`: Секунды с последней активности
- `first_seen`: Когда устройство было впервые замечено
- `num_queries`: Общее количество запросов
- `mac_vendor`: Производитель устройства
- `ips`: IP-адреса
- `name`: Имя устройства из Pi-hole
- `dhcp_expires`: Истечение аренды DHCP
- `interface`: Сетевой интерфейс

### Примеры автоматизаций

```yaml
automation:
  - alias: "Уведомить когда устройство дома"
    trigger:
      platform: state
      entity_id: device_tracker.iphone_a1b2_pihole
      to: "home"
    action:
      service: notify.mobile_app_phone
      data:
        message: "Устройство подключено к сети"

  - alias: "Уведомить когда устройство ушло"
    trigger:
      platform: state
      entity_id: device_tracker.iphone_a1b2_pihole
      to: "not_home"
    action:
      service: notify.mobile_app_phone
      data:
        message: "Устройство покинуло сеть"
```

## Устранение проблем

### Проблемы с подключением

- Проверьте доступность хоста Pi-hole: `ping <хост>`
- Проверьте сетевое соединение между Home Assistant и Pi-hole
- Убедитесь, что пароль правильный (если настроен)
- Проверьте, что API Pi-hole включен (Настройки → API → API веб-сервера)

### Устройства не отображаются

- Устройства должны иметь DNS-активность для отображения
- Проверьте настройки Pi-hole → Список сетевых устройств
- Убедитесь, что интервал обновления разумный
- Проверьте журналы Home Assistant: Настройки → Система → Журналы

### Устройства не обновляются

- Увеличьте интервал опроса, если сеть медленная
- Проверьте, что Pi-hole не перегружен
- Убедитесь, что параметр "Считать ушедшим через" подобран правильно

## Разработка

### Настройка среды разработки

```bash
# Клонируйте репозиторий
git clone https://github.com/Barma-lej/pihole-device-tracker
cd pihole-device-tracker

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

### Запуск тестов

```bash
# Запустить все тесты
pytest

# Запустить с покрытием
pytest --cov=custom_components/pihole_device_tracker

# Запустить конкретный тест
pytest tests/test_pihole_connection.py
```

### Стиль кода

```bash
# Форматирование кода
black custom_components/pihole_device_tracker

# Проверка линтера
flake8 custom_components/pihole_device_tracker

# Сортировка импортов
isort custom_components/pihole_device_tracker
```

## Вклад в разработку

Приветствуется! Пожалуйста:

1. Создайте форк репозитория
2. Создайте ветку для функций
3. Внесите изменения
4. Запустите тесты и линтер
5. Отправьте pull request

## Лицензия

Этот проект распространяется по лицензии MIT - см. файл [LICENSE](LICENSE) для подробностей.

## Поддержка

По вопросам, предложениям или проблемам, пожалуйста, создайте issue на GitHub: https://github.com/Barma-lej/pihole-device-tracker/issues
